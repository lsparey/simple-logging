simple-logging has been deployed to namespace {{ .Release.Namespace }}.

  Release:   {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}

The UI and the API are available inside the cluster at:

  http://{{ include "simple-logging.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

{{- if .Values.ingress.enabled }}

The application is exposed via Ingress at:

  {{ ternary "https" "http" (gt (len .Values.ingress.tls) 0) }}://{{ .Values.ingress.host }}

{{- else }}

To access it from outside the cluster, forward the port:

  kubectl port-forward -n {{ .Release.Namespace }} \
    svc/{{ include "simple-logging.fullname" . }} 8080:{{ .Values.service.port }}

then open http://localhost:8080

{{- end }}

{{- if not .Values.persistence.enabled }}

WARNING: persistence is disabled. Logs are stored in an emptyDir and will be
lost when the pod is restarted. Set persistence.enabled=true for production use.
{{- end }}
